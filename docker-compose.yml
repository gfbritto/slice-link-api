version: '3'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile  # Nome do seu Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - .:/app  # Isso montará seu diretório atual dentro do contêiner em /app
